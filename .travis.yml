sudo: required

env:
- DOCKER_USERNAME=kacis

services:
  - docker

script:
- sh docker-build.sh

after_success:
- if [ "$TRAVIS_BRANCH" == "master" ]; then
    docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
    docker push USER/REPO;
    fi